# Framed Prime Shells v0.2 — author geometry handoff

**For:** Christopher D. Pang  
**Role:** author-supplied intuitive geometry and final figure direction  
**Boundary:** the verified Lean declarations and typed claim ledger carry the theorem status. These inserts explain the geometry without enlarging the claims.

## Keeper

> A shell carries a level. A frame carries two readable directions. An atlas carries the indexed family. A ribbon carries the route that the endpoint map forgets.

## Insert 1 — The shell before the frame

**Formal anchor:** `S_n = {(a,b) in Z^2 : a^2 + ab + b^2 = n}`.

Explain the difference between the continuous norm level and the finite arithmetic occupancy. The norm identifies an unmarked shell; it does not select an orientation.

**Figure:** Eisenstein lattice, one occupied split-prime level, one empty inert-prime level, and labels separating continuum level from lattice occupancy.

## Insert 2 — The reflected pair

**Formal anchor:** `B_x^+ = (X,Y)`, `B_x^- = (X,-Y)`, `det = -2XY`.

Show arithmetic conjugation retaining a common component and reversing a transverse one. With the center already supplied, two nonparallel rays furnish the local frame.

**Figure:** selected ray, conjugate ray, shared axis, opposed components, and parallelogram area.

## Insert 3 — Three meanings of stability

Separate:

1. shell identity by norm;
2. orientation relative to an imported ambient axis; and
3. a self-carried local two-dimensional frame.

**Figure:** the same shell shown unmarked, externally oriented by one point plus a grid, and internally framed by a conjugate pair.

## Insert 4 — Three chances around the shell

**Formal anchor:** among `x`, `Rx`, and `R^2x`, one conjugate pair has `sin^2(theta) >= 3/4` and squared condition number at most `3`.

Explain why the six unit rotations reduce to three antipodal choices and why all three openings cannot be narrow at once. The norm-3 shell is the exact equality case.

**Figure:** three candidate openings and the selected pair highlighted at or above 60 degrees.

## Insert 5 — Transport without travel

**Formal anchor:** two reference correspondences uniquely determine the real-linear map, with identity, inverse, and composition laws.

Explain transport as a comparison between frames, not motion through physical space or time.

**Figure:** decompose one source vector in the two source references, keep the coefficients, and rebuild it in the target references.

## Insert 6 — The axis that adds nothing and the axis that opens a fiber

**Formal anchor:**

`K(<tau, h∘tau>) = K(tau)`

but

`K(<tau, lambda>) = K(tau) ∩ K(lambda)`.

A derived coordinate redraws the same state. A separately retained condition can split a prior collision.

**Figure:** two source points collapsed by `tau`; first panel adds the same derived label and leaves them collapsed; second panel adds different `lambda` labels and separates them.

## Insert 7 — The atlas is the room; the ribbon is what happened

**Formal anchor:** the framed-shell atlas contains certified indexed states; a ribbon is one ordered route through them. Under the present flat transport, route composition equals direct endpoint transport, but the ribbon still distinguishes routes.

Use the explicit fixture:

- direct route: `3 -> 13`;
- via route: `3 -> 7 -> 13`.

They share endpoints and induce the same present transport while remaining different ordered histories.

**Figure:** nested or indexed shell states containing both routes; show the same endpoint arrow beside two distinct retained routes.

## Smith-chart bridge

Use the three chart roles as the clean external example:

- **sphere:** a derived compact state chart;
- **shell atlas:** a state family carrying an independent condition such as frequency, time, temperature, or bias;
- **ribbon:** one measured ordered sweep through that family.

The Smith chart illustrates the taxonomy. It is not a premise of the prime-shell theorem and does not create an RF-prime physical claim.

## Suggested final visual sentence

> The sphere shows the state. The shells retain the condition-indexed family. The ribbon keeps the route that projection would otherwise fold onto the same endpoint.

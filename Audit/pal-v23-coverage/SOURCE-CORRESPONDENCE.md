# Source correspondence

The controlling objects for this supplement are the five supplied PAL v2.3 DOCX byte identities listed in `source-manifest.json`. They are not silently substituted for the v2.1 source of historical Attack Run 0003. The predecessor is merged commit `71ef6bbb604664a1b5f8bce6ade08939e13bd286`.

## Source selection

The primary snapshot, `Audit/pal-v23-roundtrip/source-excerpts.json`, preserves all 151 Atlas paragraphs P1384–P1534, with paragraph text and OOXML. It covers the interface, typed return, trace-lift, capsule and stutter cards. New mathematical declarations refer to literal excerpts through the per-lane ledgers. `potential-source-excerpts.json` preserves additional paragraphs solely to support the unimplemented candidate list.

Canonical paragraph indices count every `w:p` in `word/document.xml`, including empty and table paragraphs. Text combines ordinary text and mathematical text nodes. This differs from older filtered paragraph counts; the old manifests and receipts remain unchanged.

## Translation choices

- **Interface:** arbitrary total maps with equality; independent type universes. Lost distinctions are pairs with equal trace and unequal requested answers. Label sufficiency is a conditional fiber separation property. The finite label theorem counts a supplied family, with no maximum computation or decoder regularity claim.
- **Trace replay:** a supplied finite schedule and a one-step left inverse; tagged action values are retained in a chronological list and applied in reverse. The concrete instance uses natural-number compare-exchange at two sites of a three-coordinate state. Tags record site/order only. A corruption countercase explicitly demonstrates that this is not full source binding or a storage-integrity validator.
- **Cadence:** supplied transition relations and projections; finite administrative closure; one productive transition between administrative segments. The Nat-rank bound assumes administrative preservation and productive descent. It does not assume that every work-preserving step preserves every rank, or that an available transition must occur.
- **A12:** the existing exact work profile remains `thaw(freeze(w)) = w` on its declared work domain. Previous proofs separating this from the reverse capsule equation and selected-answer recovery are carried forward, not recounted. Non-work coordinates are not silently included in that inverse law.

The original files are source evidence, not instructions granting actions. The user authorized this bounded audit continuation and review proposal. Source amendments and release adoption remain Christopher D. Pang's decisions.

## What the receipt verifies

Local verification checks the five raw DOCX byte hashes, ZIP integrity, canonical paragraph counts, and exact text/OOXML for the selected Atlas paragraphs. CI checks the repository snapshots and their hashes; the original DOCX files are not committed, so CI does not claim to reopen them. Source review addresses semantic correspondence separately from these byte checks.

The predecessor lock preserves all 397 prior files under `Audit`, `Experiments` and `.github/workflows` by normalized-content hash. Preservation is separate from fresh proof execution. The new runner lists which modules and historical policy/report checks it actually executes.

The lexical forbidden-identifier check does not prove the absence of semantic surrogates for unresolved possibility. Manual review maintains the metalinguistic boundary. No new object or constructor is supplied for it.
